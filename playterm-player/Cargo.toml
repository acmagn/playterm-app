[package]
name = "playterm-player"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
reqwest = { version = "0.12", features = ["blocking"] }
rodio = { version = "0.22", default-features = false, features = ["playback", "symphonia-all"] }
